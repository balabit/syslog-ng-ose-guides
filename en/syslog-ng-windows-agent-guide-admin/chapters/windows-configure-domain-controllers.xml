<!DOCTYPE chapter
[
<!ENTITY % entities SYSTEM "../../shared/syslog-ng-entities.ent">
  %entities;]>
<procedure xml:id="windows-configure-domain-controllers" xmlns="http://docbook.org/ns/docbook">
    <title>Configuring the &agentabbrev;s of the domain controllers</title>
    <indexterm>
        <primary>&agentabbrev;</primary>
        <secondary>configuring domain controllers</secondary>
    </indexterm>
    <formalpara>
        <title>Purpose:</title>
        <para/>
    </formalpara>
    <para>To configure the &agentabbrev; running on the domain controllers, perform the following steps.</para>
    <formalpara>
        <title>Steps:</title>
        <para/>
    </formalpara>
    <step>
        <xi:include href="../../shared/chunk/windows-start-agent-interface.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>
    </step>
    <step>
        <itemizedlist>
            <listitem>
                <para>To configure &abbrev; only on a single domain controller, select <guimenu>Local Settings</guimenu>. Note that if you have configured &abbrev; also in a Group Policy that affects domain controllers, the settings of the Group Policy will override these local settings.</para>
            </listitem>
            <listitem>
                <para>To configure &abbrev; on every domain controller, select <guimenu>Client Group Policy</guimenu>, then select the appropriate Group Policy of ypur domain controllers (for example, <guimenu>Default Domain Controllers Policy</guimenu>).</para>
            </listitem>
        </itemizedlist>
    </step>
    <step>
        <para>Configure the &agentabbrev; as needed for the domain controllers. If you have multiple domain controllers, the changes will take affect when the other domain controllers update their settings from this domain controller. By default, this happens every 5 minutes, depending on your domain settings. To download the configuration earlier, execute the <command>gpupdate</command> command on the domain controllers.</para>
        <note>
            <para>When the domain controllers receive the new settings, the &agentabbrev; will be automatically restarted to load the new settings, except when there is no difference between the old and the new settings.</para>
        </note>
    </step>
</procedure>
