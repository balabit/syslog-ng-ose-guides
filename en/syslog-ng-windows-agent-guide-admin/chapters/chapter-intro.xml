<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
<!ENTITY % entities SYSTEM "../../shared/syslog-ng-entities.ent">
  %entities;]>
<chapter xml:id="chapter-intro" xmlns="http://docbook.org/ns/docbook" version="5.0">
    <title>Introduction</title>
    <indexterm>
        <primary>configuring syslog-ng</primary>
        <secondary>on Windows</secondary>
    </indexterm>
    <indexterm>
        <primary>&agentabbrev;</primary>
    </indexterm>
    <para>This chapter describes how to install and configure the &agentabbrev; on Microsoft Windows hosts.</para>
    <para>The &agent; is a log collector and forwarder application for the Microsoft Windows platform. It collects the log messages of the Windows-based host and forwards them to a syslog-ng server using regular or TLS-encrypted TCP connections.</para>
    <para>The features and restrictions of the &agentabbrev; are summarized below:</para>
    <itemizedlist>
        <listitem>
            <para>Reads messages from eventlog containers and log files.</para>
        </listitem>
        <listitem>
            <para>Transfers log messages using TCP.</para>
        </listitem>
        <listitem>
            <para>Supports TLS encryption.</para>
        </listitem>
        <listitem>
            <para>Authenticates the server using X.509 certificates. Mutual authentication is also supported.</para>
        </listitem>
        <listitem>
            <para>The format of eventlog messages can be customized using macros.</para>
        </listitem>
        <listitem>
            <para>Supports multiple destinations both in parallel and fail-over modes.</para>
        </listitem>
        <listitem>
            <para>Can be managed from a domain controller using group policies.</para>
        </listitem>
        <listitem>
            <para>Only basic filtering is supported by the agent, message segmenting, parsing, and classification is not.</para>
        </listitem>
        <listitem>
            <indexterm>
                <primary>disk buffer</primary>
                <secondary>on Windows</secondary>
            </indexterm>
            <para>Note that the log messages on Windows come from files &mdash; either eventlog containers or custom log files &mdash; which are already stored on the hard disk, so the agent does not use additional disk buffering.</para>
        </listitem>
    </itemizedlist>
    <section xml:id="windows-sect-supported-os">
        <title>Supported operating systems</title>
        <para>The central syslog-ng server cannot be installed on Microsoft Windows platforms. The syslog-ng Agent for Windows is capable of forwarding log messages to the central syslog-ng server. It is part of the syslog-ng PE, and is licensed together with it.</para>
        <para>The &agentabbrev; application supports the following operating systems. Unless explicitly noted otherwise, the subsequent releases of the platform (for example, Windows Server 2008 R2 and its service packs in case of Windows Server 2008) are also supported.</para>
        <indexterm>
            <primary>supported operating systems</primary>
        </indexterm>
        <xi:include href="../../shared/chunk/windows-supported-platforms.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>
        <note>
            <para>The &agent; application supports the XML-based eventlog format used on Microsoft Windows Vista and newer platforms. It also offers full support for 64-bit operating systems.</para>
        </note>
    </section>
</chapter>
