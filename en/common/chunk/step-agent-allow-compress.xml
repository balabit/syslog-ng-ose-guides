<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE step
 [  <!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
 %entities;]>
<step xmlns="http://docbook.org/ns/docbook" version="5.0">
    <para>Click <guimenu>Advanced Options</guimenu>.</para>
    <figure>
        <title>Adding new server</title>
        <mediaobject>
            <imageobject role="html">
                <imagedata format="PNG" fileref="win-server-advanced-options.png"/>
            </imageobject>
            <imageobject role="fo">
                <imagedata format="PNG" fileref="&imgroot;/win-server-advanced-options.png" contentwidth="10cm"/>
            </imageobject>
        </mediaobject>
    </figure>
    <para>To compress the messages during transfer to save bandwidth, select the <guimenu>Allow Compression</guimenu> option. Note that for &agentabbrev; to actually use compression, the following points must be met.</para>
    <itemizedlist>
        <listitem>
            <para>The <guimenu>Server > Advanced Options > Allow Compression</guimenu> option must be enabled.</para>
        </listitem>
        <listitem>
            <para>You must use SSL and/or RLTP to send messages to the logserver (that is, at least one of the <guimenu>Use syslog-ng proprietary Reliable Log Transfer Protocol (RLTP)</guimenu> or <guimenu>Use SSL encryption</guimenu> options must be enabled.</para>
        </listitem>
        <listitem>
            <para>The logserver must be configured to enable compression. If the logserver is &abbrev; the proper <parameter>allow-compress()</parameter> option must be enabled in the source (for details, see <olink targetdoc="syslog-ng-pe-guide-admin" targetptr="rltp-options"/> and <olink targetdoc="syslog-ng-pe-guide-admin" targetptr="tlsoptions"/>). If the logserver is &ssb;, enable the <guimenu>Log > Sources > Allow compression</guimenu> option. Note that to send compressed messages to &ssb;, you must use the <trademark>RLTP</trademark> protocol (for details, see the &doc-site;).</para>
        </listitem>
    </itemizedlist>
</step>
