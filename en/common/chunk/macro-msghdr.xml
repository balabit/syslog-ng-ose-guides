<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE simplesect
 [  <!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
 %entities;]>
<simplesect xml:id="macro-msghdr" xmlns="http://docbook.org/ns/docbook" version="5.0">
    <title>MSGHDR</title>
    <indexterm type="parameter">
        <primary>MSGHDR</primary>
    </indexterm>
    <indexterm type="parameter">
        <primary>${MSGHDR}</primary>
    </indexterm>
    <para><emphasis role="bold">Description:</emphasis> The name and the PID of the program that sent the log message in <userinput>PROGRAM[PID]:</userinput> format. Includes a trailing whitespace. Note that the macro returns an empty value if both the PROGRAM and PID fields of the message are empty.</para>
</simplesect>
