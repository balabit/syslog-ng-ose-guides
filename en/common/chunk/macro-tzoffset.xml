<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE simplesect
 [  <!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
 %entities;]>
<simplesect xml:id="macro-tzoffset" xmlns="http://docbook.org/ns/docbook" version="5.0">
    <title>TZOFFSET, R_TZOFFSET, S_TZOFFSET</title>
    <indexterm>
        <primary>TZOFFSET, R_TZOFFSET, S_TZOFFSET</primary>
    </indexterm>
    <indexterm>
        <primary>${TZOFFSET}, ${R_TZOFFSET}, ${S_TZOFFSET}</primary>
    </indexterm>
    <para><emphasis role="bold">Description:</emphasis> The time-zone as hour offset from GMT, for example: <userinput>-07:00</userinput>. In syslog-ng 1.6.x this used to be <userinput>-0700</userinput> but as <parameter>${ISODATE}</parameter> requires the colon it was added to <parameter>${TZOFFSET}</parameter> as well.</para>
</simplesect>
