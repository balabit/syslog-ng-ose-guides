<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
 [  <!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
 %entities;]>
<topic xmlns="http://docbook.org/ns/docbook" version="5.0">
    <para>If you want to use the Java-based modules of &abbrev; (for example, the Elasticsearch, HDFS, or Kafka destinations), you must compile &abbrev; with Java support.</para>
    <itemizedlist>
        <listitem>
            <para>Download and install the Java Runtime Environment (JRE), 1.7 (or newer). The Java-based modules of &abbrev; are tested and supported when using the Oracle implementation of Java. Other implementations are untested and unsupported, they may or may not work as expected.</para>
        </listitem>
        <listitem>
            <para>Install <link xmlns:ns1="http://www.w3.org/1999/xlink" ns1:href="https://gradle.org/gradle-download/">gradle</link> version 2.2.1 or newer.</para>
        </listitem>
        <listitem>
            <para>Set <userinput>LD_LIBRARY_PATH</userinput> to include the <filename>libjvm.so</filename> file, for example:<command>LD_LIBRARY_PATH=/usr/lib/jvm/java-7-openjdk-amd64/jre/lib/amd64/server:$LD_LIBRARY_PATH</command></para>
        </listitem>
        <listitem>
            <para>Use the <userinput>--with-python=2.7</userinput> compiler option during the configuration, for example, <command>./configure --with-python=2.7</command></para>
        </listitem>
    </itemizedlist>
</topic>
