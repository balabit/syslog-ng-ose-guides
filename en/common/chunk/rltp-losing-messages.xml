<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE itemizedlist
 [  <!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
 %entities;]>
<itemizedlist xmlns="http://docbook.org/ns/docbook" version="5.0" condition="pe">
    <para>In the following cases, it is possible to lose log messages even if you use <trademark>RLTP</trademark>:</para>
    <listitem>
        <para>If you use <trademark>RLTP</trademark> together with non-reliable disk-buffer, it is possible to lose logs.</para>
    </listitem>
    <listitem>
        <para>When sending logs through a relay that is using a non-reliable disk-buffer, it is possible to lose logs if the relay crashes.</para>
    </listitem>
    <listitem>
        <para>When sending logs through a relay that is using a non-reliable disk-buffer, it is possible that logs are duplicated if the relay crashes, or it is stopped.</para>
    </listitem>
    <listitem>
        <para>If the underlying disk system of &abbrev; fails to write the log messages to the disk, but it does not return a write error, or some other hardware or operating-system error happens.</para>
        <note>
            <para>To minimize the chances of losing messages, it is recommended to set the <parameter>flush-size()</parameter> parameter of the receiver to 1 (however, note that depending on your &abbrev; configuration, this can decrease the performance of the receiver).</para>
        </note>
    </listitem>
</itemizedlist>
