<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE para
[
<!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
%entities;
]>
<para xmlns="http://docbook.org/ns/docbook" version="5.0">The following destination drivers can use disk-based buffering: mongodb(), program(), smtp(), snmp(), sql(), unix-dgram() and unix-stream(). The network(), syslog(), tcp() and tcp6() destination drivers can also use disk-based buffering, except when using the <parameter>udp</parameter> transport method.</para>
