<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE warning
 [  <!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
 %entities;]>
<warning xmlns="http://docbook.org/ns/docbook" version="5.0">
    <indexterm>
        <primary>pid file</primary>
    </indexterm>
    <indexterm type="parameter">
        <primary>-p</primary>
    </indexterm>
    <indexterm type="parameter">
        <primary>--pidfile</primary>
    </indexterm>
    <para>If you already had syslog-ng Open Source Edition (OSE) installed on the host, and are upgrading to &product;, make sure that the <parameter>${SYSLOGNG_OPTIONS}</parameter> environmental variable does not contain a <parameter>-p &lt;path-to-pid-file&gt;</parameter> option. If it does, remove this option from the environmental variable, because it can prevent &abbrev; from stopping properly. Typically, the environmental variable is set in the files <filename>/etc/default/syslog-ng</filename> or <filename>/etc/sysconfig/syslog-ng</filename>, depending on the operating system you use.</para>
</warning>
