<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE warning
[
  <!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
  %entities;
  ]>
<warning xmlns="http://docbook.org/ns/docbook" version="5.0">
    <indexterm>
        <primary>XML configuration file</primary>
    </indexterm>
    <indexterm>
        <primary>MMC snap-in</primary>
    </indexterm>
    <para>If you are using an XML configuration file, or you have installed &agentabbrev; with an XML configuration file, it is not possible to use the MMC snap-in for configuring the &agentabbrev;.</para>
</warning>
