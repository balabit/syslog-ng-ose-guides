<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
[
<!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
%entities;
]>
<topic xmlns="http://docbook.org/ns/docbook" version="5.0">
    <note>
        <indexterm>
            <primary>rotating log files</primary>
        </indexterm>
        <indexterm>
            <primary>logrotate</primary>
        </indexterm>
        <para>When using this destination, update the configuration of your log rotation program to rotate these files. Otherwise, the log files can become very large.</para>
        <para>Also, after rotating the log files, reload &abbrev; using the <command>syslog-ng-ctl reload</command> command, or use another method to send a SIGHUP to &abbrev;.</para>
    </note>
</topic>
