<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE para
 [  <!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
 %entities;]>
<para xmlns="http://docbook.org/ns/docbook" version="5.0">If you are using the <parameter>flags(no-parse)</parameter> option, then syslog message parsing is completely disabled, and the entire incoming message is treated as the ${MESSAGE} part of a syslog message. In this case, &abbrev; generates a new syslog header (timestamp, host, and so on) automatically. Note that since <parameter>flags(no-parse)</parameter> disables message parsing, it interferes with other flags, for example, disables <parameter>flags(no-multi-line)</parameter>.</para>
