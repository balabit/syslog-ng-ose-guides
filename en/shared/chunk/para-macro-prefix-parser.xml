<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
<!ENTITY % entities SYSTEM "../../shared/syslog-ng-entities.ent">
%entities;
]>
<para xmlns="http://docbook.org/ns/docbook" version="5.0" xml:base="p-parser-prefix.xml">Names starting with a dot (for example, <userinput>.example</userinput>) are reserved for use by &abbrev;. If you use such a macro name as the name of a parsed value, it will attempt to replace the original value of the macro (note that only soft macros can be overwritten, see <xref linkend="macros-hard-vs-soft"/> for details). To avoid such problems, use a prefix when naming the parsed values, for example, <userinput>prefix(my-parsed-data.)</userinput></para>
