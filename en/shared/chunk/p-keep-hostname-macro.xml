<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE para
[
<!ENTITY % entities SYSTEM "../syslog-ng-entities.ent">
%entities;
]>
<para xmlns="http://docbook.org/ns/docbook" version="5.0">The <parameter>use-dns()</parameter>, <parameter>use-fqdn()</parameter>, <parameter>normalize-hostnames()</parameter>, and <parameter>dns-cache()</parameter> options will have no effect if the <parameter>keep-hostname()</parameter> option is enabled (<userinput>keep-hostname(yes)</userinput>) and the message contains a hostname.</para>
