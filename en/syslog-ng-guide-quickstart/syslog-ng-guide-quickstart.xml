<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE article
  [<!ENTITY % entities SYSTEM "../common/syslog-ng-entities.ent">
  %entities;
  ]
 >
<article xml:id="syslog-ng-guide-quickstart" xml:lang="en-EN" xmlns="http://docbook.org/ns/docbook" version="5.0">
    <info>
        <title>Quick Start Guide for &product; &version;</title>
        <orgname>&vendor;</orgname>
        <keywordset>
        	<keyword>syslog-ng, &product;, &abbrev;, logging, log collection, syslog-ng documentation, syslog-ng configuration, syslog-ng manual, system logging, eventlog collection, central log server, log management, &docgithash;</keyword>
        </keywordset>
        <subjectset>
            <subject>
                <subjectterm>&product; &version; features</subjectterm>
            </subject>
        </subjectset>
        <xi:include href="../xml-stylesheet/common/common.xml" xmlns:xi="http://www.w3.org/2001/XInclude" xpointer="common_copyright_bb"/>
        <pubdate><?dbtimestamp?></pubdate>
        <xi:include xmlns:xi="http://www.w3.org/2001/XInclude" href="../xml-stylesheet/common/common.xml" xpointer="common_logo_bb"/>
    </info>
	
    <section xml:id="introduction">
        <title>Introduction</title>
		<para>The syslog-ng application is a flexible and highly scalable system logging application that is ideal for creating centralized and trusted logging solutions.</para>
    	<xi:include xmlns:xi="http://www.w3.org/2001/XInclude" href="../syslog-ng-guide-admin/chapters/chapter-concepts.xml" xpointer="para-philosophy-intro"/>
    	
    	<section xml:id="modes_of_operation">
			<title>Modes of operation</title>
    		<xi:include xmlns:xi="http://www.w3.org/2001/XInclude" href="../syslog-ng-guide-admin/chapters/chapter-concepts.xml" xpointer="para-modes-of-operation-intro"/>
    		<xi:include xmlns:xi="http://www.w3.org/2001/XInclude" href="../syslog-ng-guide-admin/chapters/chapter-concepts.xml" xpointer="para-modes-of-operation-server"/>
    		<xi:include xmlns:xi="http://www.w3.org/2001/XInclude" href="../syslog-ng-guide-admin/chapters/chapter-concepts.xml" xpointer="para-modes-of-operation-client"/>
    		<xi:include xmlns:xi="http://www.w3.org/2001/XInclude" href="../syslog-ng-guide-admin/chapters/chapter-concepts.xml" xpointer="para-modes-of-operation-relay"/>
		</section>
    	
    	<section xml:id="scope">
    		<title>Scope</title>
    		<para>This guide contains instructions for setting up syslog-ng Premium Edition (PE) for evaluation. It covers server installation in Linux, and client installation on Linux and Windows.</para>
    		<para>In addition, basic configuration options are provided for disk buffering, reliable transfer protocol, macros in filenames, storing messages in encrypted files, and configuring syslog-ng to act as a relay.</para>
    		<para>This guide is intended as a quick introduction. For evaluating syslog-ng PE in scenarios which exceed the single client-to-server complexity (including, but not limited to usage in domain hosts, complex networks, productive environments, and load testing), refer to the syslog-ng Premium Edition 5 LTS Administrator Guide instead.</para>
    	</section>
    	
    	<section xml:id="supported_platforms">
    		<title>Supported platforms</title>
    		<para>The list of supported platforms is available here:</para>
    		<para><link xmlns:ns1="http://www.w3.org/1999/xlink" ns1:href="http://www.balabit.com/network-security/syslog-ng/central-syslog-server/specifications">http://www.balabit.com/network-security/syslog-ng/central-syslog-server/specifications</link>.</para>
    		<para>For Windows, the &agent; application is available for all Windows releases starting with Windows XP, including the 64-bit versions of the operating system.</para>
    	</section>
    </section>
	
	<section xml:id="installation">
		<title>Installation</title>
		
		<section xml:id="prerequisites">
			<title>Prerequisites</title>
			<para>The installers are available via <link xmlns:ns1="http://www.w3.org/1999/xlink" ns1:href="https://www.balabit.com/mybalabit">MyBalabit</link>. In addition to the installers, a <link xmlns:ns1="http://www.w3.org/1999/xlink" ns1:href="https://www.balabit.com/mybalabit/company-licenses">valid license</link> is required to install the &abbrev; server. Contact your sales representative for access and license files.</para>
									
			<section xml:id="downloading_linux">
				<title>Downloading the Linux installer (server and client)</title>
				<para>Obtain the &product; installer from MyBalaBit:</para>
				
				<procedure>
					<step><para>Navigate to <guimenu>Downloads > All files > syslog-ng> premium edition</guimenu></para></step>
					<step><para>Choose the latest available version (4.2.4 is used as an example)</para></step>
					<step><para>Navigate to <guimenu>Setups > linux-glibc2.3.6-amd64</guimenu></para></step>
					<step><para>Download <filename>syslog-ng-premium-edition-4.2.4-linux-glibc2.3.6-amd64.run</filename></para></step>
				</procedure>
				
				<para>The binaries include all required libraries and dependencies of syslog-ng. These components are installed in the <filename>/opt/syslog-ng</filename> directory.</para>
				<para>The installer can reuse existing configuration and license files, and also generate a simple configuration automatically into the <filename>/opt/syslog-ng/etc/syslog-ng.conf</filename> file.</para>
				<note><para>Existing syslog implementations on Linux systems are replaced during installation.</para></note>
			</section>
			
			<section xml:id="downloading_windows">
				<title>Downloading the Windows installer (client only)</title>
				<para>Obtain the &agent; installer from MyBalaBit:</para>
				
				<procedure>
					<step><para>Navigate to<guimenu>Downloads > All files > syslog-ng > syslog-ng-agent</guimenu></para></step>
					<step><para>Choose the latest available version (5.0.1a is used as an example)</para></step>
					<step><para>Navigate to <guimenu>Setups > win32</guimenu></para></step>
					<step><para>Download <filename>syslog-ng-agent-5.0.1a-setup.exe</filename></para></step>
				</procedure>
				
				<para>Regardless of the path name, the installer contains both the 32-bit and the 64-bit binaries.</para>
				<formalpara xml:id="installing_dotnet">
					<title>Installing the .NET framework (Windows only)</title>
					<para>The installer requires Microsoft .NET framework. The following versions of the framework are supported: 2.0, 3.0, or 3.5. This affects the following platforms:
						<itemizedlist>
							<listitem>
								<para>Windows XP and Windows 2003 Server: These platforms either do not contain the .NET framework, or come with an earlier, unsupported version. Install any of the supported versions.</para>
							</listitem>
							<listitem>
								<para>Windows 8 and Windows 2012 Server: These platforms come with a more recent version of the .NET framework. To ensure compatibility, install any of the supported versions.</para>
							</listitem>
						</itemizedlist>
					</para>
				</formalpara>
				<para>For further details, see <olink targetdoc="syslog-ng-windows-agent-guide-admin" targetptr="windows-install-standalone"/>.</para>
			</section>
		</section>
		
		<section xml:id="installing_server">
			<title>Installing the &abbrev; server</title>
			<para>Running &product; in server mode requires a license file. The license determines how many individual hosts can connect to the server. You can obtain the license from your sales representative.</para>
			
			<section xml:id="installing_server_linux">
				<title>Installing on Linux</title>
				
				<procedure>
					<step>
						<para>Copy the installer and <filename>license.txt</filename> file to the server.</para>
					</step>
					<step>
						<para>Execute the following command as root:</para>
						<para><command>sh syslog-ng-premium-edition-4.2.4-linux-glibc2.3.6-amd64.run</command></para>
					</step>
					<step>
						<para>Select <guimenu>Continue</guimenu> on the Welcome screen, and accept the EULA.</para>
					</step>
					<step>
						<para>Verify that the system summary is correct.</para>
						<para>If false information is displayed, your platform might not be supported. Abort installation, and if necessary, contact BalaBit for support.</para>
					</step>
					<step>
						<para>Keep the default installation path and register your installation. Existing syslog implementations on the system are replaced.</para>
					</step>
					<step>
						<para>Provide the full path to the license file (<filename>license.txt</filename>).</para>
					</step>
					<step>
						<para>The installer generates a simple configuration. Make the following changes:</para>
						<itemizedlist>
							<listitem>
								<para>Enable receiving remote log messages</para>
							</listitem>
							<listitem>
								<para>Skip entering a remote destination</para>
							</listitem>
						</itemizedlist>
						
						<formalpara>
							<title>Expected outcome</title>
							<para>The installer stops the previously installed syslog implementation, and starts the &abbrev; server.</para>
						</formalpara>
					</step>
					<step>
						<para><emphasis>Validating the installation</emphasis></para>
						<para>Test local logging:</para>
						<substeps>
							<step>
								<para>Issue the following commands as root:</para>
								<para><command>logger test message</command></para>
							</step>
							<step>
								<para>Verify local log with the following command:</para>
								<para><command>tail /var/log/messages</command></para>
								<formalpara>
									<title>Expected outcome</title>
									<para>The <computeroutput>test message</computeroutput> line is displayed in the log.</para>
								</formalpara>
							</step>
						</substeps>
					</step>
				</procedure>
			</section>
		</section>
		
		<section xml:id="installing_client">
			<title>Installing the &abbrev; client</title>
			<para>No license file is required to run &abbrev; in client mode.</para>
			
			<section xml:id="installing_client_linux">
				<title>Installing on Linux</title>
				
				<procedure>
					<step>
						<para>Execute the following command as root:</para>
						<para><command>sh syslog-ng-premium-edition-4.2.4-linux-glibc2.3.6-amd64.run</command></para>
					</step>
					<step>
						<para>Select <guimenu>Continue</guimenu> on the Welcome screen, and accept the EULA.</para>
					</step>
					<step>
						<para>Verify that the system summary is correct.</para>
						<para>If false information is displayed, your platform might not be supported. Abort installation, and if necessary, contact BalaBit for support.</para>
					</step>
					<step>
						<para>Keep the default installation path and register your installation. Existing syslog implementations on the system are replaced.</para>
					</step>
					<step>
						<para>The installer generates a simple configuration. Make the following changes:</para>
						<para>
							<itemizedlist>
							<listitem>
								<para>Disable receiving remote log messages</para>
							</listitem>
							<listitem>
								<para>Enter the IP address or host name of the &abbrev; server as remote destination</para>
							</listitem>
						</itemizedlist>
						</para>
						<formalpara>
							<title>Expected outcome</title>
							<para>The installer stops the previously installed syslog implementation, and starts the &abbrev; client.</para>
						</formalpara>
					</step>
					<step>
						<para><emphasis>Validating the installation</emphasis></para>
						<substeps>
							<step>
								<para>Test local logging. Issue the following commands as root:</para>
								<para><command>logger test message</command></para>
							</step>
							<step>
								<para>Verify local log with the following command:</para>
								<para><command>tail /var/log/messages</command></para>
								<formalpara>
									<title>Expected outcome</title>
									<para>The <computeroutput>test message</computeroutput> line is displayed in the log.</para>
								</formalpara>
							</step>
							<step>
								<para>Test remote logging. On the client machine, enter the following command:</para>
								<para><command>logger remote test message</command></para>
							</step>
							<step>
								<para>Verify the server log. On the &abbrev; server, enter:</para>
								<para><command>tail /var/log/messages</command></para>
								<formalpara>
									<title>Expected outcome</title>
									<para>The host name of the client machine and the message text <computeroutput>remote test message</computeroutput> is displayed in the log.</para>
								</formalpara>
							</step>
						</substeps>
					</step>
				</procedure>
				<formalpara>
					<title>Troubleshooting</title>
					<para>If messages are not forwarded from the client to the server, check if port 514 is blocked by a firewall (protected by default on most Linux servers).</para>
				</formalpara>
			</section>
			
			<section xml:id="installing_client_windows">
				<title>Installing on Windows</title>
				<para>The following instructions describe the standalone installation, which is configured locally. For more advanced installation options (using domain group policies, installing by group policy), refer to <olink targetdoc="syslog-ng-windows-agent-guide-admin"/>.</para>
				
				<procedure>
					<step>
						<para>Execute the downloaded binary</para>
					</step>
					<step>
						<para>Accept the EULA</para>
					</step>
					<step>
						<para>Select the destination folder for &agent;</para>
					</step>
					<step>
						<para>Choose <guimenu>Stand alone mode</guimenu></para>
					</step>
					<step>
						<para>The installer generates a simple configuration. Enter the destination IP of the &abbrev; server:</para>
						<substeps>
							<step>
								<para>Select <guimenu>Destinations</guimenu></para>
							</step>
							<step>
								<para>Double-click <guimenu>Add new server</guimenu></para>
							</step>
							<step>
								<para>Enter the server's IP address</para>
							</step>
							<step>
								<para>Change the port number to <parameter>601</parameter></para>
							</step>
							<step>
								<para>Click <guimenu>OK</guimenu></para>
							</step>
						</substeps>
					</step>
					<step>
						<para>Close the configuration window to finish installation</para>
					</step>
					<step>
						<para><emphasis>Validating the installation</emphasis></para>
						<para>Test remote logging:</para>
						<substeps>
							<step>
								<para>Log out and back in on the Windows client</para>
							</step>
							<step>
								<para>Verify the server log. On the &abbrev; server, enter the following command:</para>
								<para><command>tail /var/log/messages</command></para>
								<formalpara>
									<title>Expected outcome</title>
									<para>The logout and login events are displayed in the log.</para>
								</formalpara>
							</step>
						</substeps>
					</step>
				</procedure>
			</section>
		</section>
	</section>
	
	<section xml:id="configuration">
		<title>Configuring &abbrev;</title>
		
		<xi:include href="../common/chunk/intro-concepts.xml" xpointer="para-logging-intro1" xmlns:xi="http://www.w3.org/2001/XInclude"/>
		<xi:include href="../common/chunk/intro-concepts.xml" xpointer="para-logging-intro2" xmlns:xi="http://www.w3.org/2001/XInclude"/>
		<xi:include href="../common/chunk/intro-concepts.xml" xpointer="para-logging-intro3" xmlns:xi="http://www.w3.org/2001/XInclude"/>
		
		<para>There are many other optional elements, like filters, parsers, etc., but in this guide we focus on a core syslog-ng feature: reliable logging.</para>
		
		<section xml:id="disk_buffer">
			<title>Disk buffer</title>
			<para>The Premium Edition of syslog-ng can store messages on the local hard disk if the central log server or the network connection to the server becomes unavailable. This feature is called the disk buffer and needs to be configured only on the client side.</para>
			<para>Note that the log messages on Windows come from files – either eventlog containers or custom log files – which are already stored on the hard disk, so the agent does not use additional disk buffering.</para>
			<para>To enable disk buffering on the &abbrev; client on Linux, modify its configuration:</para>
			
			<procedure>
				<step>
					<para>Open the <filename>/opt/syslog-ng/etc/syslog-ng.conf</filename> configuration file in a text editor.</para>
				</step>
				<step>
					<para>Locate the line starting with <computeroutput>destination d_logserver</computeroutput>.</para>
				</step>
				<step>
					<para>Modify it to look like the following line:</para>
					<para><computeroutput>destination d_logserver { tcp("&lt;PEServerIP&gt;" disk-buffer(disk-buf-size(2000000)));};</computeroutput></para>
					<para>Replace <parameter>&lt;PEServerIP&gt;</parameter> with the hostname or IP address of the &abbrev; server.</para>
				</step>
				<step>
					<para>Save the configuration and restart syslog-ng.</para>
				</step>
			</procedure>
			
			<para>For additional disk buffer options, refer to <olink targetdoc="syslog-ng-guide-admin" targetptr="reference-destination-network-chapter"/>.</para>
		</section>
		
		<section xml:id="rltp">
			<title><trademark>Reliable Transfer Protocol</trademark></title>
			
			<xi:include href="../syslog-ng-guide-admin/chapters/chapter-rltp.xml" xpointer="para-rltp-intro" xmlns:xi="http://www.w3.org/2001/XInclude"/>
			
			<para>To make RLTP work, you have to enable it on the server and on all participating clients as well. In the following example, a minimum working configuration is provided; for additional options, including TLS configuration, refer to <olink targetdoc="syslog-ng-guide-admin"/>.</para>
						
			<section xml:id="configuring_server_for_rltp">
				<title>Configuring the syslog-ng PE server for RLTP</title>
				
				<procedure>
					<step>
						<para>Open the <filename>/opt/syslog-ng/etc/syslog-ng.conf</filename> configuration file in a text editor</para>
					</step>
					<step>
						<para>Replace the line <computeroutput>syslog();</computeroutput> with the following:</para>
						<para><computeroutput>syslog(port(601) transport(rltp(tls_required(no))));</computeroutput></para>
					</step>
					<step>
						<para>Save the file and restart syslog-ng</para>
						<formalpara>
							<title>Expected outcome</title>
							<para>The syslog source now supports RLTP protocol as a transport, without TLS support. Declaring the port is necessary, as there is no default port number for RLTP transport.</para>
						</formalpara>
					</step>
				</procedure>
			</section>
			
			<section xml:id="configuring_windows_client_for_rltp">
				<title>Configuring syslog-ng Windows clients for RLTP</title>
				
				<procedure>
					<step>
						<para>From the Start menu, launch the <guimenu>Configure syslog-ng Agent for Windows</guimenu> application</para>
					</step>
					<step>
						<para>Select <guimenu>Destinations</guimenu></para>
					</step>
					<step>
						<para>Right-click the previously configured destination, and choose <guimenu>Properties</guimenu></para>
					</step>
					<step>
						<para>Enable <guimenu>RLTP</guimenu></para>
					</step>
					<step>
						<para>Choose <guimenu>OK</guimenu> to save your changes, and exit from the configuration interface</para>
					</step>
					<step>
						<para>Restart syslog-ng Agent for the new configuration settings to take effect</para>
						<note><para>To restart services, you need Administrator privileges. If you use the <guimenu>Stop syslog-ng Agent</guimenu> and <guimenu>Start syslog-ng Agent</guimenu> options from the Start Menu, remember to right-click and choose the <guimenu>Run as Administrator</guimenu> option.</para></note>
					</step>
				</procedure>
				<para>Remote logging can be tested the same way as described in <xref linkend="installing_client_windows"/>.</para>
			</section>
			
			<section xml:id="configuring_linux_client_for_rltp">
				<title>Configuring syslog-ng Linux clients for RLTP</title>
				
				<procedure>
					<step>
						<para>Open the <filename>/opt/syslog-ng/etc/syslog-ng.conf</filename> configuration file in a text editor.</para>
					</step>
					<step>
						<para>Replace the line starting with <computeroutput>destination d_logserver</computeroutput> with the following:</para>
						<para><computeroutput>destination d_logserver { syslog("&lt;PEServerIP&gt;" transport(rltp()) port("601") disk-buffer(disk-buf-size(2000000)));};</computeroutput></para>
						<para>Replace <parameter>&lt;PEServerIP&gt;</parameter> with the hostname or IP address of the &abbrev; server.</para>
					</step>
					<step>
						<para>Save the file and restart syslog-ng.</para>
					</step>
				</procedure>
				<para>Remote logging can be tested the same way as described in <xref linkend="installing_client_linux"/>.</para>
			</section>
		</section>
		
		<section xml:id="macros">
			<title>Macros in filenames</title>
			<para>On servers where logs of many clients are retained for extended periods of time, log files are usually stored under a directory hierarchy. To help sort incoming log messages to such hierarchies, syslog-ng supports the use of macros. Depending on the needs of your organization, date, source host, or combined solutions can be used.</para>
			<para>In the following example, the file destination on the server is modified to also write messages into a directory structure under <filename>/var/log</filename>, where the first level is the year, the second level is the week of the year, followed by a file name based on the sending host.</para>
			
			<procedure>
				<step>
					<para>Open the <filename>/opt/syslog-ng/etc/syslog-ng.conf</filename> configuration file in a text editor</para>
				</step>
				<step>
					<para>Locate the line starting with <computeroutput>destination d_messages</computeroutput></para>
				</step>
				<step>
					<para>Modify it to look like the following line:</para>
					<para><computeroutput>destination d_messages {file(“/var/log/messages”); file("/var/log/$YEAR/$WEEK/$HOST-messages" create_dirs(yes)); };</computeroutput></para>
				</step>
				<step>
					<para>Save the file and restart syslog-ng</para>
					<note>
						<para>Collecting to <filename>/var/log/messages</filename> is left there for your convenience, it can be safely removed. Even if the related configuration item is removed, the file stays there, but it is not updated anymore.</para>
					</note>
				</step>
			</procedure>
			<para>For more details on macros available in syslog-ng, refer to the syslog-ng Premium Edition 5 LTS Administrator Guide.</para>
		</section>
	</section>
	
	<section xml:id="storing_encryption">
		<title>Storing messages in encrypted files</title>
		<para>The &abbrev; application can store log messages securely in encrypted, compressed and timestamped binary files. Timestamps can be requested from an external Timestamping Authority (TSA).</para>
		<para>Logstore files consist of individual chunks, every chunk can be encrypted, compressed, and timestamped separately. Chunks contain compressed log messages and header information needed for retrieving messages from the logstore file.</para>
		<para>The &abbrev; application generates an SHA-1 hash for every chunk to verify the integrity of the chunk. The hashes of the chunks are chained together to prevent injecting chunks into the logstore file. The &abbrev; application can encrypt the logstore using various algorithms, using the aes128 encryption algorithm in CBC mode and the hmac-sha1 hashing (HMAC) algorithm as default.</para>
		<para>In the following example, a simple logstore destination is added which stores logs with maximum compression.</para>
		
		<procedure>
			<step>
				<para>Open the <filename>/opt/syslog-ng/etc/syslog-ng.conf</filename> configuration file in a text editor</para>
			</step>
			<step>
				<para>Locate the line starting with <computeroutput>destination d_messages</computeroutput></para>
			</step>
			<step>
				<para>Add the following line right below:</para>
				<para><computeroutput>destination d_logstore { logstore("/var/log/messages.lgs" compress(9) ); };</computeroutput></para>
			</step>
			<step>
				<para>Locate the line containing <computeroutput>destination(d_messages)</computeroutput></para>
			</step>
			<step>
				<para>Add the following line right below:</para>
				<para><computeroutput>destination(d_logstore)</computeroutput></para>
			</step>
			<step>
				<para>Restart syslog-ng for the configuration changes to take effect</para>
			</step>
			<step>
				<para><emphasis>Validating the changes</emphasis></para>
				<para>You can verify that logs are arriving to the logstore using the following command:</para>
				<para><command>/opt/syslog-ng/bin/logcat /var/log/messages.lgs</command></para>
			</step>
		</procedure>
	</section>
	
	<section xml:id="relay">
		<title>3.5 syslog-ng PE as a relay</title>
		<para>As  mentioned earlier, &abbrev; can be turned into a relay. This functionality is often used on larger networks, or when logs are collected from network devices using UDP and forwarded to a central location using the more reliable TCP or RLTP protocols. When used as a relay, &abbrev; does not store the logs locally, but forwards them immediately to the central &abbrev; server.</para>
		<para>In this example, a &abbrev; Linux client is reconfigured as a relay.</para>
		
		<procedure>
			<step>
				<para>Open <filename>/opt/syslog-ng/etc/syslog-ng.conf</filename> in a text editor</para>
			</step>
			<step>
				<para>Remove the current log statement: starting with line <computeroutput>“log {“</computeroutput>, delete everything until the end of the file</para>
			</step>
			<step>
				<para>Add a new UDP source for router logs:</para>
				<para><computeroutput>source s_udp {udp();}; </computeroutput></para>
			</step>
			<step>
				<para>Add a new log path for storing local logs locally:</para>
				<para><computeroutput>log { source(s_local); destination(d_messages); };</computeroutput></para>
			</step>
			<step>
				<para>Add a new log path for sending both local messages and logs collected from the UDP source to the central server:</para>
				<para><computeroutput>log {</computeroutput></para>
				<para><computeroutput>source(s_local);</computeroutput></para>
				<para><computeroutput>source(s_udp);</computeroutput></para>
				<para><computeroutput>destination(d_logserver);</computeroutput></para>
				<para><computeroutput>};</computeroutput></para>
			</step>
			<step>
				<para><emphasis>Validating the changes</emphasis></para>
				<para>Test the relay by executing the following command on the relay machine:</para>
				<para><command>/opt/syslog-ng/bin/loggen -i -D localhost 514</command></para>
				<para>It generates about a thousand messages a second and sends to the UDP port of the local &abbrev; relay. Executing <command>tail /var/log/messages</command> should not show any of the generated messages on the relay, but doing the same on the server machine should show a large number of similar lines:</para>
				<para>
					<synopsis>Sep 20 21:18:09 relayhost prg00000[1234]: seq: 0000009458, thread: 0000, runid: 1379704679, stamp: 2013-09-20T21:18:09 PADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADDPADD</synopsis>
				</para>
			</step>
		</procedure>
	</section>
	
</article>
